<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN" "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">
<mapper namespace="mybatis.config.DeptMapper">
	<!--
	개발자는 mybatis 를 이용할 경우 자바코드에서 쿼리문을 작성하지않으면
	수많은 jdbc 관련 코드 또한 작성할 필요가 없고 오직 쿼리문만 잘하면된다
	-->
	<!--  모든글 가져오기 -->
	<select id ="selectAll" resultType="emp.model.Dept">
		select * from dept order by deptno asc
	</select>
	<!-- 한건만 가져오가 -->
	<select id="select" parameterType="int" resultType="emp.model.Dept">
		select * from dept where deptno=#{deptno}
	</select>
</mapper>